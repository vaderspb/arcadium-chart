apiVersion: v1
kind: Service
metadata:
  name: {{ include "arcadium-chart.fullname" . }}
  labels:
    {{- include "arcadium-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arcadium-chart.selectorLabels" . | nindent 4 }}
